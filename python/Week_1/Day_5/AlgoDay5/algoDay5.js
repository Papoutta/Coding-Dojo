// the client give us 2 data : keys and values
// we are going to create a function that is going to create an object with the keys and values given.
// we are going to loop from the beggining to the end inside keys and values to fill a new value that is the object expected









function zipArraysIntoMap(keys, values){ 
    var expected1 = {
        keys : values
    }
    for(var i=0; i<keys.length; i++){
        
    }
    
}

zipArraysIntoMap(["name", "age", "gender"],["farouk", 24,"male"]);